"""LOCOMO long-term conversational memory environment for MASkills.

Importing ``maskills.envs.locomo.env`` triggers ``@register_env("locomo")`` so the
env becomes addressable via ``maskills.make_env("locomo", config)``.
"""
